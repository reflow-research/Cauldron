# Rust build artifacts
cauldron/rust_tools/target/
cauldron/rust_tools/.tmp/
cauldron/rust_tools/weights-keypair.json
gatekeeper/target/
sdk/rust/target/

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/

# Node
node_modules/

# Docs (hosted separately, not shipped with the repo)
book/

# Local dev artifacts
.tmp/
.smoke-*
.code
.claude
frostbite*.txt
mapped_accounts.txt
build/
image.png

# Validation raw logs (CSVs and JSONs are tracked, raw logs are not)
docs/validation/logs/*.log
