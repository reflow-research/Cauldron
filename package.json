{
  "name": "cauldron-modelkit",
  "version": "0.4.1",
  "description": "Frostbite Cauldron modelkit (CLI + SDK)",
  "license": "UNLICENSED",
  "scripts": {
    "postinstall": "node scripts/select-runner.js --copy",
    "docs:build": "cd book && honkit build . site",
    "docs:serve": "cd book && honkit serve . --port 4000",
    "docs:serve:static": "python3 -m http.server 4000 --directory book/site"
  },
  "files": [
    "cauldron/**",
    "!cauldron/rust_tools/target/**",
    "!cauldron/**/__pycache__/**",
    "!cauldron/**/*.pyc",
    "docs/**",
    "!docs/validation/**",
    "examples/**",
    "scripts/**",
    "README.md",
    "CAULDRON.md",
    ".env.example"
  ],
  "devDependencies": {
    "honkit": "^6.1.6"
  },
  "dependencies": {
    "solders": "^0.0.1-security"
  }
}
